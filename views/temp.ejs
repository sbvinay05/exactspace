<!DOCTYPE html>
<html>
<head>
  <title>Template Page</title>
</head>
<body>
  <h1>JSON Data:</h1>
 
   {% for key, value in json_data.items() %}
            <div>
                <label>{{ key }}</label>
                <b>
                    <input type="text" value="{{ value }}" readonly>
                </b>
            </div>
        {% endfor %}
</body>
</html>
